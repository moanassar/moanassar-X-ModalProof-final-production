# Data directory

Place datasets here (or configure external dataset roots in YAML configs).

Suggested layout:

- `data/raw/` - downloaded source data
- `data/interim/` - intermediate preprocessed artifacts
- `data/processed/` - final model-ready artifacts

This repository does **not** commit large datasets.
